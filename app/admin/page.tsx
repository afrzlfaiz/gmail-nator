import AdminPanel from "./admin-panel";

export const metadata = {
  title: "Admin | Alias Relay",
  description: "Manage Gmail sources and custom domains.",
};

export default function AdminPage() {
  return <AdminPanel />;
}
